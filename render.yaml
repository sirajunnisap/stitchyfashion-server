services:
  - type: web
    name: stitchy-server
    env: node
    plan: free
    branch: master
    buildCommand: npm install && npm run build
    startCommand: npm start
    autoDeploy: true